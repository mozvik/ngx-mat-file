import { NgxMatFileDirective } from './ngx-mat-file.directive';

describe('NgxMatFileDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMatFileDirective();
    expect(directive).toBeTruthy();
  });
});
